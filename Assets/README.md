# Imported Assets

This folder is generated by `Tools/asset_import_pipeline.py`.

Expected subfolders:

- `Textures`
- `Models`
- `Audio`
- `Animations`

The importer also writes `import_manifest.tsv` for code/tools and
`import_report.md` for human review of filenames, destinations, and license
or credit notes.
